# Example: validate a certification package

```bash
npm run validate:package -- ./factory-certification
```

# Example: create empty package for a new tenant

```bash
npm run create:package -- --tenant citadel --output ./examples/citadel-certification
```

See `factory-certification/` for a complete self-certified example.
